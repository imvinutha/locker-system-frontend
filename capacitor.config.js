const config = {
  appId: 'com.locker.management',
  appName: 'Locker Management System',
  webDir: 'build',
  bundledWebRuntime: false,
  server: {
    androidScheme: 'https'
  }
};

export default config;